name: font_fit
description: "High-performance auto-sizing Text widget using binary search. Drop-in replacement that automatically fits text within constraints."
version: 0.0.2
repository: https://github.com/farookeei/font_fit

environment:
  sdk: ">=2.17.0 <4.0.0"   
  flutter: ">=2.10.0"      

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2    
